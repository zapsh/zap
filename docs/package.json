{
  "name": "zap-docs",
  "version": "1.0.0",
  "private": true,
  "description": "ZAP 面板文档站（VuePress 2）：使用指南 + API 参考",
  "scripts": {
    "gen:api": "node scripts/gen-api.mjs",
    "dev": "npm run gen:api && vuepress dev .",
    "build": "npm run gen:api && vuepress build ."
  },
  "devDependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.31",
    "@vuepress/core": "^2.0.0-rc.31",
    "@vuepress/theme-default": "^2.0.0-rc.134",
    "sass-embedded": "^1.105.0",
    "vuepress": "^2.0.0-rc.20"
  }
}
